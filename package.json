{
  "name": "bikkura-review",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2026-08-22",
    "check": "node --check app.js && node scripts/validate-data.mjs && node scripts/validate-external-data.mjs && node scripts/build-seed-sql.mjs --check && node scripts/build-external-seed-sql.mjs --check",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test:smoke": "playwright test",
    "data:update-stores": "node scripts/update-stores.mjs",
    "db:migrate": "wrangler d1 migrations apply DB --local",
    "db:seed": "node scripts/build-seed-sql.mjs && wrangler d1 execute DB --local --file=seed/seed.sql",
    "seed:external": "node scripts/validate-external-data.mjs && node scripts/build-external-seed-sql.mjs && wrangler d1 execute DB --local --file=seed/external-reports.sql",
    "build": "pnpm run check && pnpm run test:unit && node scripts/build.mjs"
  },
  "dependencies": {
    "jose": "6.1.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.62.1",
    "wrangler": "4.128.0"
  }
}
